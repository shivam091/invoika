.card.card-primary.border.border-primary
  .card-header
    .d-flex.justify-content-between.align-items-center
      %ul.nav.nav-pills.flex-column.flex-lg-row.nav-justified
        %li.nav-item
          = link_to t(".all"), quotes_path, class: "flex-sm-fill text-sm-center nav-link #{active_when(current_action?("index"))}"
        %li.nav-item
          = link_to t(".draft"), draft_quotes_path, class: "flex-sm-fill text-sm-center nav-link #{active_when(current_action?("draft"))}"
        %li.nav-item
          = link_to t(".converted"), converted_quotes_path, class: "flex-sm-fill text-sm-center nav-link #{active_when(current_action?("converted"))}"
        %li.nav-item
          = link_to t(".accepted"), accepted_quotes_path, class: "flex-sm-fill text-sm-center nav-link #{active_when(current_action?("accepted"))}"
      .d-flex.flex-column-reverse.flex-lg-row
        = render_if_exists("shared/export_dropdown")
        = link_to new_quote_path, class: "btn btn-primary ms-0 ms-lg-2 mb-2 mb-lg-0" do
          = external_svg_tag("svgs/plus.svg")
          %span.d-block.d-lg-none= button_text("new")
          %span.d-none.d-lg-block= button_text("new_quote")
  .card-body{data: {controller: "select-all-checkbox"}}
    = render_if_exists("quotes/quote")
  - if @pagy.pages > 1
    .card-footer
      = render_if_exists("shared/pagination", locals: {pagy: @pagy})
